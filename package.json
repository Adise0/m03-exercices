{
  "name": "testing",
  "version": "1.0.0",
  "main": "build/main.exe",
  "type": "module",
  "scripts": {
    "dev": "setupEnv.bat && nodemon -q . -- --execType=Auto",
    "only": "setupEnv.bat && nodemon -q . -- --execType=Only",
    "exec": "setupEnv.bat && nodemon -q .",
    "createExercise": "cd src/exercices/ && createExercise.bat"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "dependencies": {
    "clang-format": "^1.8.0"
  }
}
